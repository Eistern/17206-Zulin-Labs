#include "OptimalGamer.h"

std::vector<unsigned int> OptimalGamer::hitShip(const Board& important) const {
//    std::vector<unsigned int> choice(2);
//
//    if (important.getInfo(_previousHit) == 3) {
//        //Choose one of the adjusted squares, form choice
//    } else {
//        //Make some choice
//    }
//    std::copy(choice.begin(), choice.end(), _previousHit.begin());
//
//    return choice;
}

std::vector<unsigned int> OptimalGamer::setShip() const {
//    std::vector<unsigned int> choice(3);
//
//    return choice;
}